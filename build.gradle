/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'user' at '18. 10. 14 오전 11:16' with Gradle 2.14
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/2.14/userguide/tutorial_java_projects.html
 */

apply plugin: 'java'
apply plugin: 'application'

//Force to use utf-8
compileJava.options.encoding = "UTF-8"
compileTestJava.options.encoding = "UTF-8"

//This is unnecessary
mainClassName = 'com.storycraft.StoryPlugin'

repositories {
    mavenCentral();
}

jar {
    manifest {
        attributes (
            'Agent-Class': 'com.storycraft.StoryPlugin',
            'Premain-Class': 'com.storycraft.StoryPlugin',
            'Can-Redefine-Classes': 'true',
            'Can-Retransform-Classes': 'true'
        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

dependencies {
    // add Server jar
    compileOnly files('../Server/server.jar')

    compile 'net.sf.dozer:dozer:5.4.0'

    // Declare the dependency for your favourite test framework you want to use in your tests.
    // TestNG is also supported by the Gradle Test task. Just change the
    // testCompile dependency to testCompile 'org.testng:testng:6.8.1' and add
    // 'test.useTestNG()' to your build script.
    testCompile 'junit:junit:4.12'
}